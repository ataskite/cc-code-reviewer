# 审查输出格式规范

本文件定义代码审查报告的输出格式，供子 agent 在第五步（生成审查报告）时参考。

**标题要求（最高优先级）**：报告的第一条非空内容**必须**是一级标题 `# 代码审查报告 - {PROJECT_NAME}`（`{PROJECT_NAME}` 取自注入的项目名称）。它必须是报告的第一个有效行；前置任何空行、emoji、引用块，或直接以 `### ⚙️ 审查配置快照` 等 `##`/`###` 标题开头，都会导致飞书云文档显示为 `untitled`。该一级标题是飞书云文档标题的唯一来源（`lark-cli docs +create` 不接受 `--title`），同时也是主 skill 飞书上传的前置校验，子 agent 落盘前必须自行校验该标题行存在。「审查配置快照」等章节紧跟一级标题之后输出。

**语言要求**：整份报告必须使用中文撰写；代码标识符、配置键、类名、方法名、框架名可保留原文。

**核心排列规则**：所有问题严格按 **P0 → P1 → P2 → P3 → 待确认** 优先级排序输出。**不按维度分组**，每条问题通过「所属维度」字段标注其所属审查维度即可。

**证据规则**：每条问题必须给出可追溯证据，禁止只有纯文字结论。证据可以是代码片段、配置片段、依赖坐标、目录结构、调用链、Git diff、测试缺失证明、扫描结果或运行命令输出摘要。代码级问题优先贴代表性代码片段，并用 `// ←` 标注问题行；非代码级问题使用最能支撑结论的证据类型。

---

## 分批合并报告格式

`core/merge-batch-results.sh` 生成的分批合并报告是确定性脚本报告，不重新启动 agent 重写结论。它必须包含以下章节：

- `审查配置快照`：记录生成时间、审查类型、审查范围、审查模式、语义增强、Run ID、本轮主任务批次、已纳入合并批次、遗留批次和文件覆盖率。
- `审查范围说明`：说明项目名称、覆盖方式、覆盖口径和未纳入范围。
- `大仓库审查执行摘要`：汇总批次状态、覆盖率和去重后的已合并问题数。
- `批次状态总览`：列出所有批次状态、本轮主任务标记、合并处理、文件数、行数、模块和错误信息。
- `已完成批次发现`：只包含已纳入本次合并的 completed 批次结果，并在写入前对相同发现块做确定性去重。
- `跨批依赖线索`：汇总已纳入批次中的跨批依赖待复核内容。
- `覆盖限制与未审查范围`：说明失败、待执行、执行中、结果缺失或未纳入本轮的批次不进入正式问题结论。

分批合并报告只有在全部计划批次都已纳入合并时才能使用完整报告标题；只要存在未纳入合并的计划批次，即使这些批次已经 completed，也必须标记为 `[阶段性]`。

---

## ⚙️ 审查配置快照（报告头部必须展示）

> 本节记录外部注入的所有审查配置参数，确保报告结论与审查边界可追溯。

必须包含：
- 生成时间
- 审查类型
- 审查范围
- 审查模式
- 审查模型
- 启用维度
- 跳过维度
- 模式说明
- 检测到的技术栈
- 项目 ignore 状态：未配置 / 已启用（命中 N 条规则，过滤 M 个同类问题）
- 文件覆盖率
- 审查输入清单：`review-input.json` 路径与 selected / excluded 数量（可用时）
- 运行覆盖清单：`run-manifest.json` 路径；completed / failed / leftover 必须由该清单推导。v1.6 还必须把覆盖路径规范化为仓库相对路径，并基于稳定仓库身份、语言和相对路径生成跨 clone/workspace 一致的 `item_id`，同时保留 `coverage_sets`、typed failure class、`terminal_state` 与输入模式；旧版逐文件 `coverage` 字段继续保留用于兼容读取。

## 📊 审查范围说明
- **项目类型**
- **审查范围**
- **审查时间**
- **审查基准路径**
- **检测到的技术栈**
- **启用的审查维度**
- **跳过的审查维度**
- **项目 ignore**：展示 `.cc-code-reviewer/ignore/issues.yml` 是否启用；启用时展示命中规则数和过滤问题数
- **覆盖方式**

## 📊 执行摘要

必须包含：
- 整体结论
- 最高风险点
- 安全性 / 性能 / 可维护性 / 技术债务评分
- 问题汇总表
- 总计问题数
- 项目 ignore 命中统计：启用时必须展示过滤前候选问题数、过滤后输出问题数、过滤同类问题数

## 🔍 覆盖限制与未审查范围

必须明确：
- 已重点检查
- 未充分覆盖
- 限制说明

## ✅ 最佳实践亮点

可写亮点；如无明显亮点，可明确写未发现足够稳定且值得单独表扬的最佳实践亮点。

## P0 证据门槛

每条 P0 必须在证据、影响和建议中明确证明：生产路径可达、置信度高、后果达到事故级、没有足以阻断事故的有效防护，并且问题必须阻断发布。任一项无法证明时不得输出为 P0，应按审查模式进入 P1、待确认或更低级别。

## 问题条目通用规则（最高优先级，强制遵守）

所有 P0/P1/P2/P3/待确认 问题**逐条独立成块输出**，每一条都必须有完整的结构化字段，**禁止**用列表/一句话把多条问题压缩成一节。这是报告体量的核心来源——读者看到汇总表说有 N 个问题，正文里就必须有 N 个独立展开的问题条目。

**逐条完整输出（强制）**：

1. **每条问题用三级标题 `###` 开头**，标题格式固定为 `### {问题编号} | [维度名称] {问题一句话标题}`，例如 `### P0-1 | [维度5-安全] 所有接口无认证 + LLM 可执行 Shell 命令导致 RCE`。标题中的「问题一句话标题」必须概括本条问题，不得只写编号。**禁止**用 `**问题编号**：P0-N` 这种加粗字段代替三级标题，也**禁止**把整条问题塌缩成列表项 `- P0-1：xxx`。
2. **每条问题必须包含完整字段**：位置、置信度/所属维度、问题、证据、影响、建议（待确认项为「依据/待确认原因/建议的验证方式」）。**禁止**为了省输出而省略其中任何一段，尤其是禁止把 P1/P2/P3 的问题只写成「位置 + 一句话建议」。
3. **上下文紧张时优先压缩单条内的代码片段长度**（缩短证据代码块、合并同类证据），**绝不允许**压缩问题条目数量。若问题数过多导致单次输出接近上限，应缩短每条的证据代码片段（保留 `// ←` 问题行标注即可），而非删除问题条目或把多条合并成一句。
4. **同级问题之间用 `---` 分隔**，每条之间空一行，保证飞书云文档里每个 `###` 问题标题可点击跳转、独立成段，同时不与 `##` 级别的问题类别章节混在同一层级。
5. **同类聚合**：同类问题多处出现只算一条，位置只列一个代表处并标注同类总数（如 `A.java:45（同类问题共3处）`），不逐个列出所有位置。

---

## 🔴 P0 严重问题 (Critical Issues)

> 本节标题下紧跟 P0 证据门槛说明（见 `## P0 证据门槛` 章节）。然后**逐条**输出每个 P0 问题，每条格式严格如下。

**P0 空态输出规则（强制）**：P0 为 0 时仍必须保留本节，且在本节标题与 P1 章节之间输出以下说明，不得直接省略 P0 章节或从 P1 开始：

```text
本次未发现满足 P0 五项硬门槛的问题。已按 P0 → P1 → P2 → P3 → 待确认继续输出其余风险。
```

空态下不得生成 `### P0-0`、`### P0-N` 占位问题条目，也不得把 P1 当作 P0 输出；若存在 P1 及以下问题，应从下一节开始逐条展开。

每条格式（严格按此顺序输出，每条独立成块）：

---

### P0-N | [维度名称] {问题一句话标题}

**位置**：文件路径:行号（同类问题多处出现时，只列一个代表性位置并标注总数，如 `A.java:45（同类问题共3处）`，不逐个列出所有位置）

**置信度**：高 | **所属维度**：维度名称

**问题**：一句话描述

**证据**：
```
（仅贴一个代表性证据；代码级问题含 `// ←` 行内注释标注问题行，格式见"证据规则"）
```

**影响**：为什么重要，可能造成的后果

**建议**：修复方案，如涉及代码改动可附修复后的代码片段

---

## 🟠 P1 重要问题 (Major Issues)

每条格式（严格按此顺序输出，每条独立成块）：

---

### P1-N | [维度名称] {问题一句话标题}

**位置**：文件路径:行号（同类问题多处出现时，只列一个代表性位置并标注总数）

**置信度**：高/中/低 | **所属维度**：维度名称

**问题**：一句话描述

**证据**：
```
（仅贴一个代表性证据；代码级问题含 `// ←` 行内注释标注问题行）
```

**影响**：为什么重要

**建议**：修复方案

---

## 🟡 P2 一般问题 (Minor Issues)

每条格式（严格按此顺序输出，每条独立成块）：

---

### P2-N | [维度名称] {问题一句话标题}

**位置**：文件路径:行号（同类问题多处出现时，只列一个代表性位置并标注总数）

**置信度**：高/中/低 | **所属维度**：维度名称

**问题**：一句话描述

**证据**：
```
（仅贴一个代表性证据；代码级问题含 `// ←` 行内注释标注问题行）
```

**影响**：为什么重要

**建议**：修复建议

---

## 🔵 P3 建议 (Suggestions)

每条格式（严格按此顺序输出，每条独立成块）：

---

### P3-N | [维度名称] {问题一句话标题}

**位置**：文件路径:行号（同类问题多处出现时，只列一个代表性位置并标注总数）

**说明**：建议内容

**证据**：
```
（仅贴一个代表性证据；代码级问题含 `// ←` 行内注释标注可优化点）
```

**建议**：优化方向

---

## ⚪ 待确认项

每条格式（严格按此顺序输出，每条独立成块）：

---

### 待确认-N | [维度名称] {问题一句话标题}

**位置**：文件路径:行号（同类问题多处出现时，只列一个代表性位置并标注总数）

**置信度**：低 | **所属维度**：维度名称

**依据**：
```
（仅贴一个代表性证据；代码级疑点含 `// ←` 行内注释标注疑点）
```

**待确认原因**：为什么无法在静态分析中确认

**建议的验证方式**：如何确认或排除此风险

---

## 🎯 修复优先级

至少给出：
- P0 - 立即修复
- P1 - 尽快修复
- P2 - 计划修复
- P3 - 可选优化

## 📝 总结

必须给出：
- 一句话概括整体架构判断
- 本次模式下的核心判断
- 3 个关键行动项

**审查人**: cc-code-reviewer Agent
**报告版本**: 5.5
**审查模式**: [fast / standard / deep / security]
**模型档位**: [inherit / economy / balanced / maximum]

## 大仓库审查执行摘要

Maven 大仓库分批审查报告必须额外包含本节。合并阻塞报告、阶段性报告和完整报告都需要展示，区别在标题、批次状态总览和覆盖率说明。

分批审查报告必须包含：
- Run ID
- 审查分支
- 审查模式
- 审查模型
- 语义增强：`jdtls-lsp` 或 `maven-static`
- 语义增强使用情况：当语义增强为 `jdtls-lsp` 时，必须列出实际使用的 definition / references / implementations / call hierarchy 能力、覆盖的关键类/接口/入口点，以及失败或降级说明；当语义增强为 `maven-static` 时，说明使用 Maven 静态依赖与文本检索
- 本轮主任务批次：本轮 `RUN_BATCH_IDS` 数量 / 总批次数
- 批次完成情况：已完成 / 失败 / 待执行 / 执行中
- 已纳入本次合并批次
- 遗留批次
- 批次状态总览：批次、状态、本轮主任务、合并处理、文件数、行数、模块、错误
- Java 行规模参考
- Java 文件覆盖率
- 未完成批次说明

合并阻塞报告标题必须包含 `[合并阻塞]`。当本轮主任务批次存在 `failed`、结果文件缺失、等待超时或仍未完成批次时，必须生成合并阻塞报告并标明失败/遗留批次，不能静默跳过。

阶段性报告标题必须包含 `[阶段性]`。

完整报告仅在所有批次为 `已完成` 且纳入合并后生成。阶段性报告只合并本轮已完成批次发现，未纳入本轮或未完成批次不得进入正式问题结论，必须在批次状态总览中列为遗留。
